# Medcase SDK
Library for backend applications that wants to establish connection with Medcase API. 

# Releasing

bump version
Commit to master and tag with proper version v*-release which will be the same as version in package.json, example:
git tag v1.2.0-release
git push origin v1.2.0-release
